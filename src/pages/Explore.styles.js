import styled from "styled-components";

export const StyledHeader = styled.header`
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    
    img {
        margin: 0 1em 0 .5em;
    }
    p {
        margin: 0;
        padding: 0;
        color: #fff
    }
`